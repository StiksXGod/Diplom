# REPORT

## Что исправлено автоматически

1. Проверена и выровнена структура диплома:
   - после титульного листа добавлен раздел `СПИСОК ИСПОЛНИТЕЛЕЙ`;
   - сохранены разделы `РЕФЕРАТ`, `СОДЕРЖАНИЕ`, `ТЕРМИНЫ И ОПРЕДЕЛЕНИЯ`, `ПЕРЕЧЕНЬ СОКРАЩЕНИЙ И ОБОЗНАЧЕНИЙ`, основная часть, список источников и приложение.
2. Приведены параметры оформления LaTeX-версии:
   - поля: верхнее `2 см`, нижнее `2 см`, левое `3 см`, правое `1,5 см`;
   - заголовки разделов и подразделов унифицированы;
   - подзаголовки выровнены по левому краю с отступом;
   - сохранена нумерация страниц с основной части.
3. Исправлена сквозная нумерация в исходнике:
   - для таблиц;
   - для рисунков;
   - для формул.
4. Разделы `ТЕРМИНЫ И ОПРЕДЕЛЕНИЯ` и `ПЕРЕЧЕНЬ СОКРАЩЕНИЙ И ОБОЗНАЧЕНИЙ` переведены в строковый формат без таблиц.
5. В реферате уточнены количественные сведения:
   - `57 страниц`;
   - `28 использованных источников`;
   - `7 таблиц`;
   - `5 рисунков`;
   - `1 приложение`.
6. В основной текст добавлена ссылка на приложение А.
7. В заключении выровнена формулировка цели с введением и добавлена итоговая фраза о том, что работа выполнена в полном объеме.
8. Итоговый `DOCX` создан в файле `diploma_rl_trading.docx`.

## Что требует ручной проверки

1. В разделе `СПИСОК ИСПОЛНИТЕЛЕЙ` оставлены пометки:
   - `TODO: заполнить сведения при наличии` для консультанта;
   - `TODO: заполнить сведения при наличии` для рецензента.
2. В основном тексте отсутствует оформленная система ссылок вида `[1]`, `[2]`, `[3]`.
   - Список литературы присутствует и содержит `28` источников.
   - Если кафедра требует обязательные внутритекстовые ссылки в квадратных скобках, их нужно проставить вручную после согласования с фактическими источниками каждого фрагмента.
3. Формулы оформлены и пронумерованы, но привязка каждой формулы к конкретному источнику отдельно в тексте не выполнена.
   - Если кафедра требует обязательную ссылку на источник формулы, это нужно досмотреть вручную.
4. Раздел `ОТЗЫВ РУКОВОДИТЕЛЯ` исключен из основного потока документа, чтобы соблюсти требуемый порядок разделов.
   - Его содержимое сохранено отдельно в файле `chapters/review.tex`.

## Ограничения текущего DOCX

1. Файл `diploma_rl_trading.docx` собран как визуально точная Word-версия финального PDF по страницам.
2. Такой подход сохраняет оформление, таблицы, рисунки, формулы и нумерацию максимально близко к PDF.
3. Если нужна полноценно редактируемая Word-версия по абзацам, таблицам и формулам, потребуется отдельная ручная переработка документа в редакторе Word.

## Что было добавлено

1. `СПИСОК ИСПОЛНИТЕЛЕЙ` — добавлен как отсутствующий обязательный раздел.
2. `REPORT.md` — добавлен как отчет по автоматическим правкам и нерешенным замечаниям.

## Что сохранено без изменения научного смысла

1. Тема, предмет, объект, цель и общая логика исследования.
2. Основные результаты экспериментального моделирования.
3. Содержание глав, таблиц, рисунков и приложений без выдумывания новых фактов или источников.
